# PeerMatch — Supabase Edge Functions (verification email via Resend)
# See docs/SUPABASE-EMAIL-SETUP.md for full setup steps.

[functions.send-verification-email]
# Render calls this with SUPABASE_SERVICE_ROLE_KEY; function also checks that secret.
verify_jwt = true
